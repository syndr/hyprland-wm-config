/* ---- 💫 https://github.com/LinuxBeginnings 💫 ---- */
/* Screensaver hack picker (SUPER SHIFT L) - see ScreenHackSelect.sh */

@import "~/.config/rofi/config.rasi"


/* ---- Mainbox ---- */
mainbox {
  children:
    [ "inputbar", "message", "listview"];
}

/* ---- Entry ---- */
entry {
  expand: true;
  placeholder: " 🖥️ Search Screensaver Hacks";
}

/* ---- Listview ---- */
/* Single column: rows carry a screenshot thumbnail (when generated by
   ScreenHackShots.sh) plus "name  description". */
listview {
  columns: 1;
  lines: 9;
  fixed-height: false;
  scrollbar: true;
  spacing: 6px;
  cycle: true;
}

window {
  width: 55%;
  height: inherit;
}

/* ---- Elements ---- */
element {
  orientation: horizontal;
  spacing: 12px;
}

element-icon {
  size: 88px;
}

element-text {
    horizontal-align: 0.0;
    vertical-align: 0.5;
    margin: 0px;
    padding: 0px;
}
